# budget/

모델 사용량/비용을 일 단위로 기록.

원칙:
- 가능하면 OpenClaw `session_status` 기준으로 토큰 사용량을 기록
- 비용(₩/$)은 **플랫폼에서 단가/청구정보가 제공될 때만** 함께 기록
- 제공되지 않으면 "비용 추정 불가"로 남기고, 추후 단가가 정해지면 소급 계산 가능하도록 토큰을 남김

파일:
- `budget/daily/YYYY-MM-DD.md`
